# Recovered candidate weights

The two `.pt` files in this directory contain only the `gen.` subtree of their
source checkpoints, with the prefix removed. They are recovered candidates and
are not identified as the source of any manuscript result. See
`../PROVENANCE.md` and `provenance.json`.
